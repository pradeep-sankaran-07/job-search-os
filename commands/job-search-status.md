---
description: Show the current state of your Job Search OS setup. What's configured, what's missing, what needs attention.
---

Invoke the `job-search-diagnose` skill in standalone (reporting) mode. Print the human-readable status report with each fix-it one-liner for problems found.

Do not take any fix-it action automatically. This command is read-only — the user runs the fix commands themselves.
